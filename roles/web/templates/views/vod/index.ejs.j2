<%- include('../layout', { title: '講義動画 | EduNotes', body: `
<section class="container">
  <div class="hero">
    <span class="badge">講義動画</span>
    <h1 class="hero__title">視聴・一時停止・すぐ再開。</h1>
    <p class="hero__subtitle">各動画には要点ノートとチェックポイントがあります。</p>
  </div>

  <div class="section">
    <h2 class="section__title">最新の動画</h2>
    ${(!items || !items.length) ? `
      <div class="card">
        <p>動画がまだありません。公開までお待ちください。</p>
      </div>
    ` : `
      <div class="grid">
        ${items.map(v => `
          <article class="card">
            <div class="card__eyebrow">動画レッスン</div>
            <h3>${v.title}</h3>
            <div class="meta">
              <span>再生時間: ${v.duration || '未定'}</span>
              <span>HD配信</span>
            </div>
            <div class="progress">
              <div class="progress__bar" style="width: 0%"></div>
            </div>
            <a class="btn btn--primary" href="/vod/${v.id}">再生</a>
          </article>
        `).join('')}
      </div>
    `}
  </div>

  <div class="section card">
    <div class="card__eyebrow">学習を続ける</div>
    <h3>動画とノートを組み合わせる。</h3>
    <p class="muted">関連ノートを開いて、要点を並行して整理しましょう。</p>
    <a class="btn btn--ghost" href="/notes">ノートを開く</a>
  </div>
</section>
` }) %>
