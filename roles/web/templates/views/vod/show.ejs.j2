<%- include('../layout', { title: `${title} | EduNotes`, body: `
<section class="container">
  <div class="card">
    <a class="muted" href="/vod">動画一覧へ戻る</a>
    <h1 class="hero__title" style="font-size:2.2rem">🎥 ${title}</h1>
    <p class="muted">HD配信 · 視聴しながらノートを残せます。</p>
    <div class="video-frame" style="margin-top:16px;">
      <video controls preload="metadata" style="width:100%; display:block;">
        <source src="${url}" type="video/mp4">
        お使いのブラウザは動画再生に対応していません。
      </video>
    </div>
  </div>
</section>
` }) %>
