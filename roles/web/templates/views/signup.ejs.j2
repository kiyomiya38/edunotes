<%- include('layout', { title: '新規登録 | EduNotes', body: `
<section class="container">
  <div class="hero">
    <span class="badge">学習を始める</span>
    <h1 class="hero__title">学習習慣を整える。</h1>
    <p class="hero__subtitle">レッスン、進捗、ノートを一つの流れで管理します。</p>
  </div>

  <div class="section split">
    <div class="card">
      <div class="card__eyebrow">できること</div>
      <h2>コース完走に必要な機能をすべて。</h2>
      <ul class="meta">
        <li>学習ダッシュボード</li>
        <li>タグ付きノートとコメント</li>
        <li>レッスンごとの進捗確認</li>
        <li>VODへすぐアクセス</li>
      </ul>
      <div class="card">
        <strong>学習の約束</strong>
        <p class="muted">復習しやすいまとめと要点を用意します。</p>
      </div>
    </div>

    <div class="auth-card">
      <h2>アカウント作成</h2>
      ${error ? `<p style="color:#c00">${error}</p>` : ''}
      <form class="form" method="post" action="/signup">
        <input type="hidden" name="_csrf" value="${csrfToken}">
        <label>メールアドレス</label>
        <input name="email" type="email" required>
        <label>パスワード</label>
        <input name="password" type="password" required minlength="6">
        <button class="btn btn--primary" type="submit">登録する</button>
      </form>
      <p class="muted">すでにアカウントをお持ちの方は <a href="/login">ログイン</a></p>
    </div>
  </div>
</section>
` }) %>
