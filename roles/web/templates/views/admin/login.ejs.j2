<%- include('../layout', { title: '講師スタジオ | EduNotes', body: `
<section class="container">
  <div class="hero">
    <span class="badge">講師スタジオ</span>
    <h1 class="hero__title">コースと動画を管理。</h1>
    <p class="hero__subtitle">教材の登録、公開、学習状況の把握をまとめて行えます。</p>
  </div>

  <div class="section split">
    <div class="card">
      <div class="card__eyebrow">スタジオ概要</div>
      <h2>教材をひとつの場所で管理。</h2>
      <p class="muted">テキストと動画を作成し、受講者向けに公開できます。</p>
      <div class="pill-group">
        <span class="badge">作成ツール</span>
        <span class="badge">公開</span>
        <span class="badge">品質管理</span>
      </div>
    </div>
    <div class="auth-card">
      <h2>管理者ログイン</h2>
      ${error ? `<p style="color:#c00">${error}</p>` : ''}
      <form class="form" method="post" action="/admin/login">
        <input type="hidden" name="_csrf" value="${csrfToken}">
        <label>メールアドレス</label>
        <input name="email" type="email" required>
        <label>パスワード</label>
        <input name="password" type="password" required>
        <button class="btn btn--primary" type="submit">ログイン</button>
      </form>
      <p class="muted">受講生の方は <a href="/login">学習ポータルへ</a></p>
    </div>
  </div>
</section>
` }) %>
