<%- include('../layout', { title: '新規ノート | EduNotes', body: `
<section class="container">
  <div class="hero">
    <span class="badge">新規ノート</span>
    <h1 class="hero__title">学んだ内容を記録。</h1>
    <p class="hero__subtitle">要約・重要語・次のアクションを残して復習しやすく。</p>
  </div>

  <div class="section card">
    <form class="form" method="post" action="/notes">
      <input type="hidden" name="_csrf" value="${csrfToken}">
      <label>タイトル</label>
      <input type="text" name="title" required>
      <label>本文（Markdown）</label>
      <textarea name="body_md" rows="12" placeholder="# レッスンの要約

- 重要ポイント
- 具体例
- 次のアクション"></textarea>
      <label>
        <input type="checkbox" name="is_public"> このノートを公開する
      </label>
      <div style="display:flex; gap:12px; flex-wrap:wrap;">
        <button class="btn btn--primary" type="submit">保存</button>
        <a class="btn btn--ghost" href="/notes">キャンセル</a>
      </div>
    </form>
  </div>
</section>
` }) %>
