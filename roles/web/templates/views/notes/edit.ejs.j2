<%- include('../layout', { title: '繝弱・繝育ｷｨ髮・| EduNotes', body: `
<section class="container">
  <div class="hero">
    <span class="badge">繝弱・繝育ｷｨ髮・/span>
    <h1 class="hero__title">蟄ｦ鄙偵Γ繝｢繧呈紛縺医ｋ縲・/h1>
    <p class="hero__subtitle">隕狗峩縺励ｄ縺吶￥縲∵ュ蝣ｱ繧呈紛逅・＠縺ｦ譖ｴ譁ｰ縺励∪縺吶・/p>
  </div>

  <div class="section card">
    <form class="form" method="post" action="/notes/${n.id}">
      <input type="hidden" name="_csrf" value="${csrfToken}">
      <label>繧ｿ繧､繝医Ν</label>
      <input type="text" name="title" value="${n.title.replace(/"/g,'&quot;')}" required>
      <label>譛ｬ譁・ｼ・arkdown・・/label>
      <textarea name="body_md" rows="12">${(n.body_md || '').replace(/</g,'&lt;')}</textarea>
      <label>
        <input type="checkbox" name="is_public" ${n.is_public ? 'checked' : ''}> 縺薙・繝弱・繝医ｒ蜈ｬ髢九☆繧・      </label>
      <div style="display:flex; gap:12px; flex-wrap:wrap;">
        <button class="btn btn--primary" type="submit">螟画峩繧剃ｿ晏ｭ・/button>
        <a class="btn btn--ghost" href="/notes/${n.id}">繧ｭ繝｣繝ｳ繧ｻ繝ｫ</a>
      </div>
    </form>
  </div>
</section>
` }) %>
