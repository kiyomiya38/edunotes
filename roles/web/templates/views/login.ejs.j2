<%- include('layout', { title: 'ログイン | EduNotes', body: `
<section class="container">
  <div class="hero">
    <span class="badge">学習ポータル</span>
    <h1 class="hero__title">次のレッスンを続けましょう。</h1>
    <p class="hero__subtitle">進捗の確認、動画の再開、学びの記録をひとつに。</p>
  </div>

  <div class="section split">
    <div class="card">
      <div class="card__eyebrow">選ばれる理由</div>
      <h2>コース・ノート・進捗をまとめて管理。</h2>
      <div class="grid">
        <div class="stat-card">
          <strong>学習パス</strong>
          <p class="muted">テーマ別に学び、週間目標でペース管理。</p>
        </div>
        <div class="stat-card">
          <strong>講師ノート</strong>
          <p class="muted">要点とまとめをすぐに確認。</p>
        </div>
        <div class="stat-card">
          <strong>続きから再開</strong>
          <p class="muted">中断した場所からすぐに学習再開。</p>
        </div>
        <div class="stat-card">
          <strong>共有の学び</strong>
          <p class="muted">コメントで理解を深める。</p>
        </div>
      </div>
    </div>

    <div class="auth-card">
      <h2>ログイン</h2>
      ${error ? `<p style="color:#c00">${error}</p>` : ''}
      <form class="form" method="post" action="/login">
        <input type="hidden" name="_csrf" value="${csrfToken}">
        <input type="hidden" name="next" value="${typeof next !== 'undefined' && next ? next : '/courses'}">
        <label>メールアドレス</label>
        <input name="email" type="email" required>
        <label>パスワード</label>
        <input name="password" type="password" required>
        <button class="btn btn--primary" type="submit">ログイン</button>
      </form>
      <p class="muted">はじめての方は <a href="/signup?next=${encodeURIComponent(typeof next !== 'undefined' && next ? next : '/courses')}">新規登録</a></p>
      <p class="muted">管理者の方は <a href="/admin/login">講師スタジオへ</a></p>
    </div>
  </div>
</section>
` }) %>
