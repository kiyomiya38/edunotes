<%- include('../layout', { title: 'コース | EduNotes', body: `
<section class="container">
  <div class="hero">
    <span class="badge">コース一覧</span>
    <h1 class="hero__title">学習パスを選ぶ。</h1>
    <p class="hero__subtitle">短いレッスンと講師ノートで、進捗を保ちながら学べます。</p>
  </div>

  <div class="section">
    <h2 class="section__title">注目のコース</h2>
    ${(!courses || !courses.length) ? `
      <div class="card">
        <p>コースがまだありません。公開までお待ちください。</p>
      </div>
    ` : `
      <div class="grid">
        ${courses.map(c => `
          <article class="card">
            <div class="card__eyebrow">テキストコース</div>
            <h3>${c.title}</h3>
            <p class="muted">${c.summary || '要点とまとめを整理したレッスンで学習をサポートします。'}</p>
            <div class="meta">
              <span>講師: コアチーム</span>
              <span>全8レッスン</span>
            </div>
            <div>
              <span class="badge">スタート</span>
            </div>
            <div class="progress">
              <div class="progress__bar" style="width: 0%"></div>
            </div>
            <a class="btn btn--primary" href="/courses/${c.id}">開く</a>
          </article>
        `).join('')}
      </div>
    `}
  </div>

  <div class="section split">
    <div class="card">
      <div class="card__eyebrow">学習トラック</div>
      <h3>目標に合った学習計画を作る。</h3>
      <p class="muted">カリキュラムに沿って次のレッスンを確認できます。</p>
      <a class="btn btn--ghost" href="/docs">学習ガイドを見る</a>
    </div>
    <div class="card">
      <div class="card__eyebrow">講師紹介</div>
      <h3>田中アレックス</h3>
      <p class="muted">クラウドアーキテクト。現場のワークフローを重視した講義。</p>
      <span class="badge">高評価</span>
    </div>
  </div>
</section>
` }) %>
